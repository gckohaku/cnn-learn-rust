[package]
name = "cnn-learn-rust"
version = "0.1.0"
edition = "2024"

[dependencies]
ndarray = { version = "0.16", default-features = false, features = ["std", "rayon", "blas"] }
ndarray-ndimage = "0.5"

[profile.dev]
opt-level = 1
overflow-checks = false

[profile.release]
opt-level = 3